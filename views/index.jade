extends layout

block content
  h1= title
  p Welcome to #{title}

  button(onclick= "window.location.href = '/about'") About Us
  button(onclick= "window.location.href = '/room/Example Room'") Create Room
